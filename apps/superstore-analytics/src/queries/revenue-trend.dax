EVALUATE
CALCULATETABLE(
    SUMMARIZECOLUMNS(
        DimDate[year],
        DimDate[month],
        "MonthStart", DATE(MAX(DimDate[year]), MAX(DimDate[month]), 1),
        "Sales", [Total Sales],
        "Profit", [Total Profit]
    )
    /*__FILTERS__*/
)
ORDER BY DimDate[year], DimDate[month]
